# Copy to .env.local for local development

# PostgreSQL connection string (same DB as gcp-universal-backend)
DATABASE_URL=postgresql://user:password@localhost:5432/gub

# Local dev IAP bypass — set to your email to skip the IAP header check
IAP_DEV_EMAIL=you@yourdomain.com
